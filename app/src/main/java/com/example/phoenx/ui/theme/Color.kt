package com.example.phoenx.ui.theme

import androidx.compose.ui.graphics.Color

// Fonds
val BackgroundPrimary = Color(0xFF1A1A1F)    // anthracite profond — jamais noir pur
val BackgroundSecondary = Color(0xFF242429)
val SurfaceCard = Color(0xFF2E2E35)          // + 40% opacité pour effet verre dépoli

// Accent unique — LA couleur du phénix
val AccentPrimary = Color(0xFFC97B3A)        // braise/doré
val AccentSecondary = Color(0xFFE8A85F)      // version claire pour hover/focus

// Textes
val TextPrimary = Color(0xFFF2EDE8)          // blanc chaud — jamais blanc pur
val TextSecondary = Color(0xFF9B9590)
val TextTertiary = Color(0xFF5C5855)

// États
val Success = Color(0xFF4CAF50)
val Error = Color(0xFFE57373)
val Warning = Color(0xFFFFB74D)
